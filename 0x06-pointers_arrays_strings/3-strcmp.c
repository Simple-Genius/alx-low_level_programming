#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * strcmp - a function that compares two strings
 *
 *@s1: string parameter
 *@s2: string parameter
 *
 * Return: Always 0
 */
int _strcmp(char *s1, char *s2)
{
	int val = strcmp(s1, s2);

	return (val);
}
