#include "main.h"
#include<stdio.h>
#include <string.h>
/**
 * _strlen - a function that returns the length of a string.
 *
 *
 *@s: The string parameter
 *
 * Return: Always 0
 */
int _strlen(char *s)
{
	int l = 0;
	while(*s != '\0')
	{
		l++;
		s++;
	}
	return length;
}
