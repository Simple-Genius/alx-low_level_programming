#include<stdio.h>
#include <stdlib.h>
/**_abs - Returns the absolute value of a number
 * 
 * @b: the number to be checked
 * Return: Always 0 (Success)
 */

int _abs(int);

int _abs(int b);
{
	return (abs(b));
}
